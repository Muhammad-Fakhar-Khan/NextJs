
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
<>
        <h2>Nav Bar</h2>
        {/* If we want to use Navbar on every page its better to use the main layout because the main layout is visible to every page */}
</>

  );
}
